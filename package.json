{
  "name": "nosubvod-twitch",
  "version": "1.1.0",
  "description": "Show sub-only VOD on Twitch - Modular Architecture",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\"",
    "dev": "npm run clean && npm run watch",
    "release": "node release.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "esbuild": "^0.25.11",
    "typescript": "^4.9.5"
  }
}
